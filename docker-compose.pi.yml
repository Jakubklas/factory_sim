# Device-specific compose for: pi
# Auto-generated — do not edit by hand

services:
  plc_002:
    build:
      context: .
      dockerfile: simulator/Dockerfile
    image: ghcr.io/jakubklas/factory_sim/simulator:latest
    environment:
      PLANT_CONFIG: /config
      SIM_PLC_ID: plc_002
      SIM_TICK_MS: 100
      OPCUA_HOST: plc_002
      PKI_DIR: /data/pki
    volumes:
      - ./config:/config:ro
      - plc_002_data:/data
    ports:
      - "4841:4841"
    restart: unless-stopped

volumes:
  plc_002_data:
