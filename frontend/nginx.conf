server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — all non-asset routes serve index.html.
    location / {
        try_files $uri /index.html;
    }
}
