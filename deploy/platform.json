{
  "components": {
    "backend": {
      "deploy_target": "ec2",
      "description": "API + connectors, needs access to all PLCs",
      "ports": ["3001"]
    },
    "frontend": {
      "deploy_target": "ec2",
      "description": "Web UI served from a separate server, needs access to backend APIs",
      "ports": ["8080"],
      "public_url": "https://factory-sim-ec2.coat-augmented.ts.net:8443"
    }
  }
}